[project]
name = "video2md"
version = "0.1.0"
description = "CLI pipeline for turning video speech into Markdown knowledge notes."
requires-python = ">=3.10"
readme = "README.md"

[project.scripts]
video2md = "video2md.__main__:main"

[tool.setuptools]
packages = ["video2md"]
